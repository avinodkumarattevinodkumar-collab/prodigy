<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Interactive Navigation Menu</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      height: 2000px; /* scrolling కోసం */
    }

    /* Navigation bar */
    .navbar {
      position: fixed;
      top: 0;
      width: 100%;
      background: #333;
      padding: 15px;
      display: flex;
      justify-content: center;
      transition: background 0.3s;
      z-index: 1000;
    }

    .navbar.scrolled {
      background: #0066cc; /* scroll అయ్యాక color change */
    }

    .navbar a {
      color: white;
      text-decoration: none;
      margin: 0 15px;
      font-size: 18px;
      transition: color 0.3s;
    }

    .navbar a:hover {
      color: yellow; /* hover చేయగానే color change */
    }
  </style>
</head>
<body>
  <div class="navbar" id="navbar">
    <a href="#">Home</a>
    <a href="#">About</a>
    <a href="#">Services</a>
    <a href="#">Contact</a>
  </div>

  <script>
    window.addEventListener("scroll", function () {
      const navbar = document.getElementById("navbar");
      if (window.scrollY > 50) {
        navbar.classList.add("scrolled");
      } else {
        navbar.classList.remove("scrolled");
      }
    });
  </script>
</body>
</html>
